# Copy this file to .env and adjust as needed for local development.

ENVIRONMENT=development

# Comma-separated list of allowed frontend origins for CORS.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Maximum upload size accepted by POST /api/analyze, in megabytes.
MAX_UPLOAD_SIZE_MB=20

# Google Gemini API key. Powers LLM-powered explanations, /api/chat, and the
# Gemini OCR fallback that transcribes scanned PDFs (no embedded text layer)
# before they reach the deterministic analysis pipeline.
# Leave blank to disable all of the above (deterministic analysis is used
# instead, and scanned PDFs are rejected with an honest validation error).
# NEVER commit a real key here — this file is safe to commit.
# GEMINI_API_KEY=your-key-here
