# Copy this file to .env and adjust as needed for local development.

ENVIRONMENT=development

# Comma-separated list of allowed frontend origins for CORS.
# The frontend currently runs on Vite (default http://localhost:5173).
# http://localhost:3000 is also allowed in case a Next.js dev server is used.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Maximum upload size accepted by POST /api/analyze, in megabytes.
MAX_UPLOAD_SIZE_MB=20
